import React from "react";
import {
    StyleSheet, Text, View,
    ActivityIndicator,
    SafeAreaView, Button, Image
} from "react-native";

const UserDetailView = () => {
    return(
        <Text style={{justifyContent: 'center'}}>Welcome on the UserDetailView</Text>
    );
}

export default UserDetailView;
